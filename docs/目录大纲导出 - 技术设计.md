# 目录大纲导出功能 - 技术设计文档

**文档版本**: 1.0
**创建时间**: 2026-03-28
**创建人**: 技术负责人 (TL)
**状态**: 📝 待评审
**项目**: Minidoc

---

## 一、技术选型

### 1.1 Markdown 解析方案

| 方案 | 优点 | 缺点 | 选择 |
|------|------|------|------|
| **正则表达式解析** | 零依赖、轻量、快速 | 无法处理复杂 Markdown 语法 | ✅ 选用 |
| marked/markdown-it | 功能完整、支持扩展 | 增加 bundle 体积 (~20KB) | ❌ 不选 |
| cherry-markdown 内置 | 项目已有依赖 | API 不直接暴露解析能力 | ❌ 不选 |

**技术决策**: 使用正则表达式解析 Markdown 标题
- 项目已有依赖 `cherry-markdown`，但其解析器 API 不直接可用
- 标题解析逻辑简单（`#` 开头 + 空格），正则足够处理
- 零额外依赖，保持项目轻量

### 1.2 锚点 ID 生成算法

参考 GitHub/GitLab 的锚点生成规则：

```typescript
function generateAnchorId(text: string): string {
  return text
    .toLowerCase()
    .replace(/[^\w\s-]/g, '')      // 移除特殊字符
    .replace(/\s+/g, '-')          // 空格替换为连字符
    .replace(/-+/g, '-')           // 多个连字符合并为一个
    .trim();
}
```

### 1.3 导出格式技术实现

| 格式 | 实现方式 | 技术要点 |
|------|---------|---------|
| **Markdown** | 字符串拼接 | 使用 `-` 列表 + `[text](#anchor)` 锚点链接 |
| **TXT** | 字符串拼接 | 空格缩进表示层级，可选编号 |
| **JSON** | `JSON.stringify()` | 包含完整层级结构 + 元数据 |

### 1.4 下载/复制技术实现

| 功能 | 实现方案 | 降级处理 |
|------|---------|---------|
| **文件下载** | `Blob` + `<a download>` | 新窗口打开内容 |
| **复制到剪贴板** | `navigator.clipboard.writeText()` | `document.execCommand('copy')` |

---

## 二、技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                     ExportDialog                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Tab 导航：文档导出 | 目录大纲                    │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│  ┌──────────────────────▼──────────────────────┐       │
│  │         TOCOutlineTab (新增组件)             │       │
│  │  ┌─────────────────────────────────────┐    │       │
│  │  │  格式选择：Markdown / TXT / JSON     │    │       │
│  │  └─────────────────────────────────────┘    │       │
│  │  ┌─────────────────────────────────────┐    │       │
│  │  │  配置选项：最大层级 / 标题编号        │    │       │
│  │  └─────────────────────────────────────┘    │       │
│  │  ┌─────────────────────────────────────┐    │       │
│  │  │  预览区 (滚动)                        │    │       │
│  │  └─────────────────────────────────────┘    │       │
│  │  ┌─────────────────────────────────────┐    │       │
│  │  │  [复制] [导出文件]                    │    │       │
│  │  └─────────────────────────────────────┘    │       │
│  └─────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ TOCParser    │  │ TOCGenerator │  │ TOCExporter  │
│ (解析器)     │  │ (生成器)     │  │ (导出器)     │
└──────────────┘  └──────────────┘  └──────────────┘
```

### 2.2 模块分层

```
┌─────────────────────────────────────┐
│          Presentation Layer         │
│  - ExportDialog (改造)              │
│  - TOCOutlineTab (新增)             │
│  - TOCPreview (新增)                │
└─────────────────────────────────────┘
                    │
┌───────────────────▼───────────────────┐
│          Business Logic Layer         │
│  - TOCGenerator (生成逻辑)            │
│  - FormatConverter (格式转换)         │
└───────────────────────────────────────┘
                    │
┌───────────────────▼───────────────────┐
│          Core Utility Layer           │
│  - TOCParser (Markdown 解析)           │
│  - AnchorGenerator (锚点生成)         │
│  - FileDownloader (文件下载)          │
│  - ClipboardHelper (剪贴板)          │
└───────────────────────────────────────┘
```

### 2.3 数据流

```
用户点击导出
       │
       ▼
┌─────────────┐
│ ExportDialog│
└──────┬──────┘
       │
       ▼
┌─────────────────┐     ┌──────────────┐
│ TOCOutlineTab   │────▶│ useFileStore │ (获取当前文档内容)
└────────┬────────┘     └──────────────┘
         │
         ▼
┌─────────────────┐
│ TOCParser       │ (解析 Markdown 为 TOCItem[])
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ TOCGenerator    │ (生成树状结构)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ FormatConverter │ (根据选择生成 Markdown/TXT/JSON)
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌──────┐  ┌────────┐
│复制  │  │导出文件 │
└──────┘  └────────┘
```

---

## 三、数据结构设计

### 3.1 核心接口定义

```typescript
// src/utils/toc/types.ts

/**
 * 目录项接口
 */
export interface TOCItem {
  level: number;           // 1-6，对应 H1-H6
  text: string;            // 标题文本
  line: number;            // 原始行号（从 1 开始）
  id: string;              // 生成的锚点 ID
  children: TOCItem[];     // 子标题（树状结构）
}

/**
 * 解析选项
 */
export interface ParseOptions {
  maxLevel?: number;       // 最大解析层级，默认 6
  includeLineNumbers?: boolean; // 是否包含行号，默认 true
}

/**
 * 生成选项
 */
export interface GenerateOptions {
  format: 'markdown' | 'txt' | 'json';
  maxLevel?: number;       // 最大导出层级
  includeNumbers?: boolean; // 是否包含标题编号（仅 TXT）
  indentType?: 'space' | 'tab'; // 缩进类型，默认 'space'
  indentSize?: number;     // 每级缩进空格数，默认 2
}

/**
 * JSON 导出结构
 */
export interface TOCJSON {
  title: string;
  sourceFile: string;
  generatedAt: string;
  toc: TOCItem[];
}
```

### 3.2 示例数据结构

**输入 Markdown**:
```markdown
# 第一章
## 1.1 概述
## 1.2 背景
### 1.2.1 历史
# 第二章
```

**解析结果 (TOCItem[])**:
```typescript
[
  {
    level: 1,
    text: "第一章",
    line: 1,
    id: "第一章",
    children: [
      {
        level: 2,
        text: "1.1 概述",
        line: 2,
        id: "11-概述",
        children: []
      },
      {
        level: 2,
        text: "1.2 背景",
        line: 3,
        id: "12-背景",
        children: [
          {
            level: 3,
            text: "1.2.1 历史",
            line: 4,
            id: "121-历史",
            children: []
          }
        ]
      }
    ]
  },
  {
    level: 1,
    text: "第二章",
    line: 5,
    id: "第二章",
    children: []
  }
]
```

---

## 四、核心算法设计

### 4.1 目录解析算法

```typescript
// src/utils/toc/parser.ts

/**
 * 解析 Markdown 内容为目录项列表
 */
export function parseTOC(markdown: string, options: ParseOptions = {}): TOCItem[] {
  const { maxLevel = 6, includeLineNumbers = true } = options;
  const lines = markdown.split('\n');
  const flatItems: TOCItem[] = [];

  // 步骤 1: 逐行解析，提取标题
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const match = line.match(/^(#{1,6})\s+(.+)$/);

    if (!match) continue;

    const level = match[1].length;
    if (level > maxLevel) continue;

    const text = match[2].trim();
    flatItems.push({
      level,
      text,
      line: includeLineNumbers ? i + 1 : 0,
      id: generateAnchorId(text),
      children: []
    });
  }

  // 步骤 2: 构建树状结构
  return buildTree(flatItems);
}

/**
 * 将扁平列表转换为树状结构
 */
function buildTree(flatItems: TOCItem[]): TOCItem[] {
  const root: TOCItem[] = [];
  const stack: TOCItem[] = [];

  for (const item of flatItems) {
    const newNode: TOCItem = { ...item, children: [] };

    // 弹出栈中所有层级 >= 当前节点的节点
    while (stack.length > 0 && stack[stack.length - 1].level >= item.level) {
      stack.pop();
    }

    // 添加到父节点或根
    if (stack.length === 0) {
      root.push(newNode);
    } else {
      stack[stack.length - 1].children.push(newNode);
    }

    stack.push(newNode);
  }

  return root;
}

/**
 * 生成锚点 ID
 */
export function generateAnchorId(text: string): string {
  return text
    .toLowerCase()
    .replace(/[^\w\s-\u4e00-\u9fff]/g, '')  // 保留中文
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
    .trim();
}
```

### 4.2 目录生成算法

```typescript
// src/utils/toc/generator.ts

/**
 * 根据选项生成目录字符串
 */
export function generateTOC(
  items: TOCItem[],
  options: GenerateOptions
): string {
  const { format } = options;

  switch (format) {
    case 'markdown':
      return generateMarkdown(items, options);
    case 'txt':
      return generateTXT(items, options);
    case 'json':
      return generateJSON(items, options);
    default:
      throw new Error(`不支持的格式：${format}`);
  }
}

/**
 * 生成 Markdown 格式目录
 */
function generateMarkdown(items: TOCItem[], options: GenerateOptions): string {
  const { maxLevel = 6, indentSize = 2 } = options;
  const lines: string[] = [];

  function traverse(node: TOCItem, depth: number) {
    if (node.level > maxLevel) return;

    const indent = ' '.repeat(depth * indentSize);
    lines.push(`${indent}- [${node.text}](#${node.id})`);

    for (const child of node.children) {
      traverse(child, depth + 1);
    }
  }

  for (const item of items) {
    traverse(item, 0);
  }

  return lines.join('\n');
}

/**
 * 生成 TXT 格式目录
 */
function generateTXT(items: TOCItem[], options: GenerateOptions): string {
  const { maxLevel = 6, includeNumbers = false, indentType = 'space', indentSize = 2 } = options;
  const lines: string[] = [];
  const counters: number[] = []; // 用于编号 [1, 1, 1]

  function traverse(node: TOCItem, depth: number) {
    if (node.level > maxLevel) return;

    // 更新当前层级的计数器
    counters[depth] = (counters[depth] || 0) + 1;
    counters.splice(depth + 1); // 清空子层级计数器

    // 计算缩进
    const indent = indentType === 'space'
      ? ' '.repeat(depth * indentSize)
      : '\t'.repeat(depth);

    // 构建标题文本
    let text = node.text;
    if (includeNumbers) {
      const number = counters.slice(0, depth + 1).join('.');
      text = `${number} ${text}`;
    }

    lines.push(`${indent}${text}`);

    for (const child of node.children) {
      traverse(child, depth + 1);
    }
  }

  for (const item of items) {
    traverse(item, 0);
  }

  return lines.join('\n');
}

/**
 * 生成 JSON 格式目录
 */
function generateJSON(items: TOCItem[], options: GenerateOptions, filename: string): string {
  const tocJSON: TOCJSON = {
    title: filename.replace(/\.md$/, ''),
    sourceFile: filename,
    generatedAt: new Date().toISOString(),
    toc: items
  };

  return JSON.stringify(tocJSON, null, 2);
}
```

### 4.3 导出工具函数

```typescript
// src/utils/toc/exporter.ts

/**
 * 下载文件
 */
export function downloadFile(
  content: string,
  filename: string,
  mimeType: string
): Promise<void> {
  return new Promise((resolve, reject) => {
    try {
      const blob = new Blob([content], { type: mimeType });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
      resolve();
    } catch (error) {
      // 降级处理：新窗口打开
      const newWindow = window.open('', '_blank');
      if (newWindow) {
        newWindow.document.write(content);
        newWindow.document.title = filename;
        resolve();
      } else {
        reject(new Error('无法打开新窗口'));
      }
    }
  });
}

/**
 * 复制到剪贴板
 */
export async function copyToClipboard(content: string): Promise<void> {
  // 优先使用现代 API
  if (navigator.clipboard?.writeText) {
    try {
      await navigator.clipboard.writeText(content);
      return;
    } catch (error) {
      console.warn('navigator.clipboard 失败，使用降级方案');
    }
  }

  // 降级方案：使用 execCommand
  const textarea = document.createElement('textarea');
  textarea.value = content;
  textarea.style.position = 'fixed';
  textarea.style.opacity = '0';
  document.body.appendChild(textarea);
  textarea.select();

  try {
    const success = document.execCommand('copy');
    if (!success) {
      throw new Error('execCommand 失败');
    }
  } finally {
    document.body.removeChild(textarea);
  }
}
```

---

## 五、文件结构

### 5.1 新增文件列表

```
src/
├── utils/
│   └── toc/
│       ├── index.ts         # 统一导出入口
│       ├── parser.ts        # Markdown 解析器
│       ├── generator.ts     # 目录生成器
│       ├── exporter.ts      # 导出工具
│       └── types.ts         # 类型定义
├── components/
│   └── dialogs/
│       └── ExportDialog.tsx # 改造（新增 TOCOutlineTab）
│   └── toc/
│       ├── TOCPreview.tsx   # 目录预览组件
│       └── TOCOutlineTab.tsx # 目录大纲 Tab 组件
```

### 5.2 ExportDialog 改造

```typescript
// 改造后的 ExportDialog.tsx 结构
import { TOCOutlineTab } from '@/components/toc/TOCOutlineTab';

type ExportTab = 'document' | 'toc';

export function ExportDialog() {
  const [activeTab, setActiveTab] = useState<ExportTab>('document');

  return (
    <div className="...">
      {/* Tab 导航 */}
      <Tabs value={activeTab} onValueChange={setActiveTab}>
        <TabsList>
          <TabsTrigger value="document">📄 文档导出</TabsTrigger>
          <TabsTrigger value="toc">📑 目录大纲</TabsTrigger>
        </TabsList>

        <TabsContent value="document">
          {/* 原有文档导出内容 */}
        </TabsContent>

        <TabsContent value="toc">
          <TOCOutlineTab />
        </TabsContent>
      </Tabs>
    </div>
  );
}
```

---

## 六、组件设计

### 6.1 TOCOutlineTab 组件

```typescript
interface TOCOutlineTabProps {
  // 无 props，从 store 获取当前文档
}

interface TOCOutlineTabState {
  format: 'markdown' | 'txt' | 'json';
  maxLevel: number;        // 1-6
  includeNumbers: boolean;
  tocItems: TOCItem[];     // 解析后的目录
  previewContent: string;  // 生成的预览内容
}
```

**组件结构**:
```
TOCOutlineTab
├── FormatSelector       # 格式选择 (Markdown/TXT/JSON)
├── OptionsPanel         # 选项面板
│   ├── MaxLevelSelect   # 最大层级下拉
│   └── NumberToggle     # 标题编号开关
├── TOCPreview          # 预览区
│   └── 滚动容器 (160px)
└── ActionButtons
    ├── CopyButton      # 复制按钮
    └── ExportButton    # 导出文件按钮
```

### 6.2 TOCPreview 组件

```typescript
interface TOCPreviewProps {
  content: string;       // 生成的目录内容
  format: 'markdown' | 'txt' | 'json';
}
```

---

## 七、边界处理

### 7.1 异常情况处理

| 场景 | 检测条件 | 处理方式 | UI 反馈 |
|------|---------|---------|--------|
| 空文档 | `markdown.length === 0` | 禁用导出按钮 | "当前文档为空" |
| 无标题 | `tocItems.length === 0` | 显示空状态 | "文档中没有可导出的标题" |
| 复制失败 | `copyToClipboard` 异常 | 降级为手动选择 | "请手动选择复制" |
| 下载失败 | `downloadFile` 异常 | 新窗口打开 | 新窗口展示内容 |

### 7.2 代码块内标题处理

```typescript
// 检测是否在代码块内
function isInCodeBlock(lines: string[], lineIndex: number): boolean {
  let inCodeBlock = false;
  for (let i = 0; i < lineIndex; i++) {
    if (lines[i].startsWith('```')) {
      inCodeBlock = !inCodeBlock;
    }
  }
  return inCodeBlock;
}

// 解析时跳过代码块内的标题
if (isInCodeBlock(lines, i)) continue;
```

---

## 八、性能设计

### 8.1 性能指标

| 指标 | 目标值 | 测量方式 |
|------|-------|---------|
| 解析时间 | < 50ms (1000 行文档) | `performance.now()` |
| 生成时间 | < 30ms | `performance.now()` |
| 响应时间 | < 100ms (点击到预览) | 用户感知 |

### 8.2 优化策略

1. **懒解析**: 仅在 Tab 切换或内容变化时解析
2. **缓存**: 缓存最近一次的解析结果
3. **防抖**: 配置变更时防抖 200ms 再生成预览

```typescript
// 缓存示例
const tocCache = new Map<string, TOCItem[]>();

export function parseTOCWithCache(
  markdown: string,
  options: ParseOptions
): TOCItem[] {
  const cacheKey = `${markdown}-${JSON.stringify(options)}`;

  if (tocCache.has(cacheKey)) {
    return tocCache.get(cacheKey)!;
  }

  const result = parseTOC(markdown, options);
  tocCache.set(cacheKey, result);
  return result;
}
```

---

## 九、测试策略

### 9.1 单元测试

| 测试模块 | 测试内容 |
|---------|---------|
| `parser.ts` | 标题识别、层级构建、代码块过滤 |
| `generator.ts` | Markdown/TXT/JSON 格式生成 |
| `exporter.ts` | 文件下载、剪贴板复制 |

### 9.2 测试用例示例

```typescript
// src/utils/toc/__tests__/parser.test.ts

describe('parseTOC', () => {
  it('应正确解析 H1-H6 标题', () => {
    const markdown = `
# H1
## H2
### H3
#### H4
##### H5
###### H6
    `;
    const result = parseTOC(markdown);
    expect(result).toHaveLength(6);
    expect(result.map(r => r.level)).toEqual([1, 2, 3, 4, 5, 6]);
  });

  it('应过滤代码块内的标题', () => {
    const markdown = `
# 标题 1
\`\`\`
# 这不是标题
\`\`\`
# 标题 2
    `;
    const result = parseTOC(markdown);
    expect(result).toHaveLength(2);
  });

  it('应正确处理层级跳跃', () => {
    const markdown = `
# H1
#### H4 (跳跃到 H4)
    `;
    const result = parseTOC(markdown);
    expect(result).toHaveLength(2);
    expect(result[1].level).toBe(4);
  });
});
```

---

## 十、技术风险

### 10.1 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 正则解析不精确 | 低 | 中 | 添加充分测试覆盖边界情况 |
| 中文锚点兼容性 | 低 | 低 | 使用 Unicode 范围保留中文 |
| 大文档性能 | 低 | 低 | 添加性能监控和缓存 |
| 浏览器兼容性 | 低 | 中 | 提供降级方案 |

### 10.2 兼容性列表

| 浏览器 | 版本要求 |
|--------|---------|
| Chrome | 66+ (clipboard API) |
| Safari | 13+ |
| Firefox | 63+ |
| Edge | 79+ |

---

## 十一、依赖关系

### 11.1 上游依赖

| 依赖 | 来源 | 用途 |
|------|------|------|
| `useFileStore` | `@/stores/fileStore` | 获取当前文档内容 |
| `currentFile` | `@/stores/fileStore` | 获取文件名用于下载命名 |

### 11.2 下游依赖

| 依赖 | 使用者 | 用途 |
|------|--------|------|
| `TOCOutlineTab` | `ExportDialog` | Tab 内容组件 |
| `parseTOC` | `TOCGenerator` | 解析 Markdown |
| `generateTOC` | `TOCOutlineTab` | 生成预览内容 |

---

## 十二、交付物清单

| 交付物 | 状态 | 路径 |
|-------|------|------|
| 技术架构文档 | ✅ 已完成 | 本文档 |
| 功能任务清单 | ✅ 已完成 | 见下文 |
| 核心代码 | ⏳ 待开发 | `src/utils/toc/` |
| 组件代码 | ⏳ 待开发 | `src/components/toc/` |
| 单元测试 | ⏳ 待开发 | `src/utils/toc/__tests__/` |

---

## 附录：功能任务清单

```markdown
## 功能任务清单

### 任务 1: 核心工具函数开发
- **负责人**: 全栈开发 1
- **交付物**:
  - `src/utils/toc/types.ts` - 类型定义
  - `src/utils/toc/parser.ts` - Markdown 解析器
  - `src/utils/toc/generator.ts` - 目录生成器
  - `src/utils/toc/exporter.ts` - 导出工具函数
  - `src/utils/toc/index.ts` - 统一导出入口
- **验收标准**:
  - 单元测试覆盖率 > 90%
  - 正确解析 H1-H6 标题
  - 正确处理代码块内标题过滤
  - 支持三种格式生成 (Markdown/TXT/JSON)
- **预计工时**: 4 小时

### 任务 2: 组件开发
- **负责人**: 全栈开发 2
- **交付物**:
  - `src/components/toc/TOCPreview.tsx` - 预览组件
  - `src/components/toc/TOCOutlineTab.tsx` - Tab 组件
  - `src/components/dialogs/ExportDialog.tsx` - 改造 (添加 Tab 导航)
- **验收标准**:
  - 设计还原度 > 95% (对比 .pen 设计稿)
  - 支持 Tab 切换
  - 预览区支持滚动
  - 复制/导出按钮功能正常
  - 深色模式适配
- **预计工时**: 4 小时

### 任务 3: 集成与异常处理
- **负责人**: 全栈开发 1
- **交付物**:
  - 边界情况处理 (空文档、无标题)
  - 错误提示 Toast
  - 降级方案实现
- **验收标准**:
  - 空文档显示友好提示
  - 复制失败时降级为手动选择
  - 下载失败时新窗口打开
  - 所有异常场景有 UI 反馈
- **预计工时**: 2 小时

### 任务 4: 自测报告
- **负责人**: 全栈开发 1 + 全栈开发 2
- **交付物**: `docs/目录大纲导出 - 自测报告.md`
- **验收标准**:
  - 功能演示录屏
  - 边界情况测试记录
  - 性能测试结果
- **预计工时**: 1 小时
```

---

## 代码审查清单

```markdown
## 代码审查清单

### 核心逻辑审查
- [ ] TOC 解析算法正确处理 H1-H6 层级
- [ ] 树状结构构建逻辑正确 (父子关系)
- [ ] 锚点 ID 生成规则与 GitHub/GitLab 一致
- [ ] 三种导出格式生成正确 (Markdown/TXT/JSON)

### 安全审查
- [ ] 无 XSS 风险 (标题文本正确转义)
- [ ] 文件下载无路径遍历风险
- [ ] 剪贴板操作正确处理权限

### 错误处理审查
- [ ] 空文档有友好提示
- [ ] 无标题文档有友好提示
- [ ] 复制失败有降级方案
- [ ] 下载失败有降级方案

### 代码质量审查
- [ ] TypeScript 类型定义完整
- [ ] 单元测试覆盖率 > 90%
- [ ] 代码注释清晰
- [ ] 符合项目代码规范

### 性能审查
- [ ] 解析时间 < 100ms (1000 行文档)
- [ ] 实现了解析结果缓存
- [ ] 配置变更有防抖处理

### 可访问性审查
- [ ] 支持键盘操作 (Tab、Enter、Escape)
- [ ] 按钮有 aria-label
- [ ] 颜色对比度符合 WCAG 2.1

### 发布确认
- [ ] 所有代码审查通过
- [ ] 单元测试全部通过
- [ ] 自测报告审核通过
- [ ] 可以进入测试阶段
```

---

**下一步**: 将此文档和功能任务清单交付给全栈开发团队开始实现。
